spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mokaGestComDB
    username: postgres
    password: mokasoft
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  application:
    name: INVENTORY-MANAGEMENT
server:
  port: 8090
